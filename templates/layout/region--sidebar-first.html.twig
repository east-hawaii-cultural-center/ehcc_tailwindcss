{#
/**
 * @file
 * Theme override to display a region.
 *
 * Available variables:
 * - content: The content for this region, typically blocks.
 * - attributes: HTML attributes for the region <div>.
 * - region: The name of the region variable as defined in the theme's
 *   .info.yml file.
 *
 * @see template_preprocess_region()
 */
#}
{% if content %}
  <aside{{ attributes.addClass(
    'md:w-1/4 lg:w-1/5',
    '[&_h2]:uppercase [&_h2]:text-lg [&_h2]:lg:text-xl [&_h2]:text-neutral-500 [&_h2]:font-bold [&>*]:md:mb-4',
    '[&_ul]:border-l-4 [&_ul]:border-neutral-400 [&_ul]:pl-2',
    '[&_ul>li]:p-2'
  ) }} role="complementary">
    {{ content }}
  </aside>
{% endif %}
